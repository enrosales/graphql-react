# esto seria como los endpoints de una restApi 

type Query {
  pong: String!
  messages: [Message!]
}

type Mutation {
  createMessage( title: String, content: String, author: String ): Message
}

type Message {
  _id: ID!
  title: String!
  content: String!
  author: String!
}